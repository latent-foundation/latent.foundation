#!/usr/bin/env bash
set -eu

if command -v powershell.exe >/dev/null 2>&1; then
    # GitHub Desktop on Windows uses a minimal shell PATH; PowerShell has the full PATH.
    powershell.exe -NoProfile -NonInteractive -Command "just fmt-check"
else
    just fmt-check
fi
